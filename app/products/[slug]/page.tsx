import Link from 'next/link';
import {products,getProduct,productSlug,categoryMeta} from '../data';
export function generateStaticParams(){return products.map(p=>({slug:productSlug(p)}));}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)return <main className="section"><div className="container"><h1 className="serif">Product not found</h1><Link href="/products">Back to catalogue</Link></div></main>;return <main className="product-page"><header className="product-header"><div className="container"><Link className="logo" href="/">PAUL GLOBAL</Link><Link className="btn btn-gold" href="/#contact">Request a Quote ↗</Link></div></header><section className="section"><div className="container"><Link href="/products" className="back-link">← Back to products</Link><div className="product-hero"><div><div className="eyebrow">{categoryMeta[p.category]?.number} · {p.category} · {p.type}</div><h1 className="serif">{p.name}</h1><p>{p.summary}</p></div><div className="product-hero-mark">PG<br/><small>IN-HOUSE<br/>MANUFACTURED</small></div></div><div className="product-detail-grid"><div className="detail-card"><div className="eyebrow">Applications</div><ul>{p.applications.map(x=><li key={x}>{x}</li>)}</ul></div><div className="detail-card dark"><div className="eyebrow">Technical / commercial</div><ul>{p.benefits.map(x=><li key={x}>{x}</li>)}</ul></div></div><div className="manufacturing-callout"><div><div className="eyebrow">Not a trading label</div><h2 className="serif">Manufactured in-house.<br/>Built on technical depth.</h2><p>Paul Global presents products from an in-house manufacturing operation. For buyers, that means a direct connection to the people, processes and technical knowledge behind the products.</p></div><Link className="btn btn-gold" href="/#technical">See our technical capability ↗</Link></div></div></section></main>}
