'use client';
import {useMemo,useState} from 'react';
import Link from 'next/link';
import {products,categories,slugify} from './data';
export default function ProductCatalogue(){const [cat,setCat]=useState('All');const [q,setQ]=useState('');const filtered=useMemo(()=>products.filter(p=>(cat==='All'||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase())),[cat,q]);return <><div style={{display:'flex',gap:12,flexWrap:'wrap',margin:'45px 0 20px'}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..." style={{flex:'1 1 280px',padding:'15px 18px',border:'1px solid #d6ddd5',borderRadius:999,background:'#fff',fontSize:14}}/><select value={cat} onChange={e=>setCat(e.target.value)} style={{padding:'15px 20px',border:'1px solid #d6ddd5',borderRadius:999,background:'#fff',fontSize:14}}><option>All</option>{categories.map(c=><option key={c}>{c}</option>)}</select></div><div style={{fontSize:12,color:'#738078',marginBottom:16}}>{filtered.length} products / solutions</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:14}}>{filtered.map((p,i)=><Link href={`/products/${slugify(p.name)}`} key={`${p.category}-${p.name}`}><article style={{background:'#fff',minHeight:210,padding:26,border:'1px solid #e0e5df',transition:'transform .2s,box-shadow .2s'}}><div style={{display:'flex',justifyContent:'space-between',fontSize:10,letterSpacing:'.14em',color:'#8a958e'}}><span>{String(i+1).padStart(2,'0')}</span><span>{p.category}</span></div><h3 className="serif" style={{fontSize:28,margin:'30px 0 10px'}}>{p.name}</h3><p style={{color:'#68746d',fontSize:13,lineHeight:1.6,marginBottom:24}}>{p.summary}</p><span style={{fontSize:12,fontWeight:700}}>View details ↗</span></article></Link>)}</div></>}
